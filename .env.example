# Required for the People Assistant (/api/assistant) serverless function.
# Set this as a real environment variable in the Vercel project settings for
# production; for local testing of the function with `vercel dev`, copy this
# file to .env and fill in a real key (never commit the real key).
ANTHROPIC_API_KEY=sk-ant-...
